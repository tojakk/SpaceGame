///scr_accelerate
if gamepad_button_check(player,button_Rtrig)
{
    speed += m_speed;
/*
    if (direction > 0 and direction < 90) //upper right quadrant
    {
        hspeed += m_speed;
        vspeed -= m_speed;
    }
    else if (direction = 90) // up
    {
        vspeed -= m_speed;
    }
    else if (direction > 90 and direction < 180) //upper left quadrant
    {
        hspeed -= m_speed;
        vspeed -= m_speed;
    }
    else if (direction = 180) //left
    {
        hspeed -= m_speed;
    }
    else if (direction > 180 and direction <270) //lower left quadrant
    {
        hspeed -= m_speed;
        vspeed += m_speed;
    }
    else if (direction = 270) //down
    {
        vspeed += m_speed;
    }
    else if (direction > 270) //lower right quadrant
    {
        hspeed += m_speed;
        vspeed += m_speed;
    }
    else if (direction = 0)
    {
        hspeed += m_speed;
    }
*/
}
